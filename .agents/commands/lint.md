静的解析を実行し、結果を報告してください。

## 手順

1. 以下のコマンドを実行する:
   ```
   fvm dart run melos run lint
   ```

2. 結果を整理して報告する:
   - エラー・警告が **0件** の場合: 「✅ lint パス」と報告
   - 問題がある場合: ファイルパスと行番号を含め一覧化し、修正を提案する

## 注意

`info` レベルの警告も含め、**すべてゼロ** になるまで修正が必要です。
警告が残ったままコミットすると CI が失敗します。
