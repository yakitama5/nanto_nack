name: fvm_skeleton_template
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  logger: ^2.6.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_quality_lints: ^0.9.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
