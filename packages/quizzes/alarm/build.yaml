# slang_build_runner は i18n JSONファイルのみをスキャン対象とする
# lib/src 以下のクイズ実装ファイルは対象外とすることで生成を高速化
targets:
  $default:
    sources:
      include:
        - assets/i18n/**
        - lib/gen/**
        - $package$
      exclude:
        - lib/gen/**/*.g.dart
